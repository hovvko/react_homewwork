import {Counter} from "./components";

function App() {
    return (
        <div style={{display: 'flex'}}>
            <Counter/>
            <Counter/>
            <Counter/>
        </div>
    );
}

export default App;
